class Item {
    constructor(itemId, name, description, price) {
        this.itemId = itemId;
        this.name = name;
        this.description = description;
        this.price = price;
    }
}

export default Item;
